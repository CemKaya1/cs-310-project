name: cs_310_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # UI / Fonts
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8

  # Image picker
  image_picker: ^1.0.7

  # 🔥 FIREBASE (TAM SET)
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.5.0
  firebase_storage: ^12.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/core/mock/mock_images/
